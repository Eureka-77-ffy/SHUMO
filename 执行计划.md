# 2026 国赛 A 题《药材的烘干问题》执行计划 V2

本计划依据用户提供的赛题、两个数据附件、完整调研报告，以及用户提出的物理解释与可信度风险清单修订。V2 将物理机制、无量纲时间尺度、模型时间线和独立验证列为明确任务与验收门槛。当前M0—M3已完成，M4数值总验收也已完成：端面条件审查、Q1非线性独立抽检、统一误差台账、输入留出检验和result1—result4逐单元格核验均通过；下一步进入图表生成，完整论文、论断对应与终审尚未完成。

## 当前进度

- M0完成：输入审计、代表状态尺度、无量纲方程、四问模型契约、独立Python环境、机器可读配置与代数检查。详见 [阶段0验收](reports/stage0_acceptance.md)。
- 已确定有效闭合：ρ经验式用于有效体积热容；干基输运使用材料守恒的干质量测度。严格总密度、固定长度和守恒干质量的同时解释与附件末期半径不兼容，证明见 [模型契约](reports/model_contract.md)。
- M1完成：共享标量内核、Q1全场和论文表1—2、Bessel热场、网格/容差/有效步长、累计平衡、8组敏感性及Excel回读。详见 [Q1验收与机制解释](reports/q1_acceptance.md)。实际配置为表面加密1280格，并用2560格复核；不声称所有四位小数末位均稳定。
- M2完成：Q23双向耦合、全域事件、论文表3—5、完整result2/result3、网格/有效步长/修正热学收支、参数与延拓情景、独立有限元至阈值。主情景临界57.4740 h；1280→2560格差0.076 s，独立细步终点差0.0021 s，均不作为严格误差上界。详见 [Q3验收与误差报告](reports/q3_acceptance.md) 和 [Q2前三小时报告](reports/q2_acceptance.md)。
- M3完成：Q4从原始初态开始，固定半径退化、纯收缩/制造解、独立移动域与B/C反事实独立复核均通过；临界51.0920 h，严格整数秒183932 s，实际半径1.2 cm且无需外推。result4的44287个数值与23165个域外空白逐项回读通过。详见 [Q4验收与因素分离](reports/q4_acceptance.md)。
- M4本次数值审查完成：Q1原题非线性独立FEM抽检通过；29个二维端面情景形成17组同网格对照；270条统一误差证据分列数值、收支、参数/边界、端面情景及7类未量化项。见 [端面审查](reports/endface_assessment.md) 和 [统一预算](reports/error_budget.md)。
- M4结果总验收完成：8,882,410个工作簿数值格、23,165个Q4域外空白格逐项回读；四问均与独立FEM公共时空点交叉核验，Q1另有解析基线；输入留一/隔点留出、量纲边界、守恒残差、灵敏度和极端反例统一汇总。见 [最终验证报告](reports/final_result_validation.md)。
- 审查发现：同样暴露端面的情景下，Q2中截面温度差约0.0017℃、Q3终点提前约7—8 s，超过相应四位显示尺度；Q4终点变化约−0.0284 s。保留已冻结的一维结果并明确中截面/端面假设，禁止无条件声称忽略端面不影响四位精度；本次不把粗二维绝对值替换为正式答案。
- 下一步先从未舍入产物生成科研图表，再补充论断证据表并将适用条件嵌入模型与结果章节，最后完成真实文献、论文与终审。数值通过和条件审查不替代真实物理校准；如确定改以暴露端面为正式模型，需另行提高二维绝对解精度并重新验收导出。

## 修订依据：原计划是否踩中陷阱

结论：部分踩中。V1 对若干逻辑错误已有明确防范，但仍以求解器和交付文件组织工作，物理解释缺少验收要求，独立验证也没有覆盖完整主模型。

| 用户指出的风险 | V1 实际情况 | V2 修订 |
|---|---|---|
| 过程与主导机制未讲清 | 部分命中：有方程与任务链，缺少分阶段机制假说及证据 | 新增机制问题、诊断量、竞争机制对照和结论成立条件 |
| 将数值工具当成模型创新 | 有表述风险：“主模型”栏直接写 FVM/BDF；尚未直接声称其原创 | 分列物理模型与计算工具；贡献表述必须对应证据 |
| 无量纲分析停留在 Bi | 明显缺项：V1 没有独立的时间尺度任务 | 增加内部/外部响应尺度、Fo、α/D、环境驱动与收缩时间尺度，并要求解释模型取舍 |
| Q1—Q4 时间线混用 | 已有防范：Q2/Q4 从初态重算，Q3 复用 Q2 | 新增时间线契约与自动断言，区分题目编号和实际过程阶段 |
| Q3 只看中心或五个表格点 | 已有防范：全域重建最大值与阈值事件 | 增加最大位置轨迹、阈值前后复核及空间漏检检查 |
| Q4 重复计入对流、干基守恒不闭合 | 已要求推导，但缺少直接排错算例 | 明列材料速度 v、网格速度 w 与相对速度；新增纯收缩、退化与制造解验证 |
| 空气量与干基含水率直接等同 | 已声明等效边界假设，但未评估该假设影响 | 新增量纲/参照基准清单、边界映射情景和不可辨识性说明 |
| 精确输出缺乏充分验证 | 已有收敛、守恒、扰动与 Q1 半解析；Q2/Q4 独立空间离散不足 | 补充实际步长约束收敛，独立弱形式求解器覆盖 Q23/Q4；建立误差预算 |

“已有防范”仅评价计划设计，不代表验证已经通过。V2 执行顺序为：解释过程与提出可检验判断 → 推导并冻结模型 → 实现求解 → 独立验证与解释结果 → 生成论文和结果文件。

## 1. 输入、目标与实施范围

- 赛题：[A题.pdf](inputs/official/A题.pdf)。
- 数据：[附件1.xlsx](inputs/official/附件1.xlsx)、[附件2.xlsx](inputs/official/附件2.xlsx)。
- 输出模板：原材料目录中的 `result1.xlsx`—`result4.xlsx`。
- 调研依据：[research_report.md](inputs/reference/research_report.md)。
- 实施目录：当前 Workspace 根目录；首次制订计划时为空，现已包含完整论文、程序、结果和验收记录。
- 实施后端：按报告采用 Python、NumPy、SciPy、openpyxl；数据图采用 Python；论文采用中文国赛 LaTeX 模板。
- 最终目标：四问模型与结果、六张题定论文表、四个结果工作簿、数值验证与敏感性分析、可复现代码、论文源文件和 PDF。

调研报告提及的另一份 `A题_各问模型选择与整体方法建议.md` 未在本次材料中提供，不将其视作已核验的依据。报告中的检索引用标记不能直接用于参考文献；正式写作时重新检索、核验并生成真实 BibTeX。获奖与发表潜力评价不作为执行验收标准。

## 2. 物理模型、待验证机制与计算工具

| 问题 | 物理/数学模型 | 计算工具 | 必须回答的机制问题 |
|---|---|---|---|
| Q1 | 恒定热物性的径向热传导；D(C) 控制的水分扩散；二者解耦 | 环形 FVM、刚性积分、Bessel 校验 | 为什么热响应与水分迁移速度不同？环境升温和内部传导各起什么作用？ |
| Q2 | 附录三物性随状态变化的双向热质耦合 | FVM、BDF、非线性通量 | 升温促进扩散与降含水率抑制扩散如何竞争？何时温场接近平台？ |
| Q3 | Q2 全域最大含水率首次通过阈值的事件问题 | 空间最大值搜索、时间求根 | 什么控制最终达标？表层阻力、内部梯度与边界交换各占什么地位？ |
| Q4 | 附录四物性与指定收缩几何下的材料输运 | 材料坐标、移动域 FVM、独立弱形式对照 | 几何路径缩短、表面积/体积比变化和物性替换如何共同影响干燥？ |

FVM、BDF、Radau、Kirchhoff 通量和已有坐标变换均按数值工具或已知方法准确归属。论文贡献只写已完成且有证据支持的过程解释、适用条件、终点误差与几何—物性作用分析；不以方法名称或组合数量认定创新。

主积分器采用 BDF，选取代表性场景用 Radau 复核；提供稀疏 Jacobian 或其结构。SciPy 支持上述刚性积分器、连续输出和事件定位；事件依赖步内符号变化，因此还需在阈值附近限制步长并检查遗漏。[SciPy 官方文档](https://docs.scipy.org/doc/scipy/reference/generated/scipy.integrate.solve_ivp.html)

Q1 在本题所选简化条件下是热场与水分场解耦。Q2 从 t=0、T=28℃、C=2.55 重新积分，不能接续 Q1 的 1800 s 状态。Q3 复用 Q2 的未舍入状态；Q4 同样从 t=0 重算。

### 2.1 先建立过程解释，再用仿真检验

温度边界驱动表面换热，温度梯度推动内部传导；含水率差驱动表面排湿，内部含水率梯度推动水分补给表面。在 Q2/Q4 中，局部 T 升高使经验式中的 D 增大，局部 C 降低使 D 减小；C 又改变 ρ、cp、k，从而影响后续温度响应。温场与水分场的这条反馈链是模型内的机理关系，不能表述为从单条附件曲线识别出的实验因果效应。

潜热未进入主模型，因此不能用主模型证明蒸发冷却可忽略，也不能将模型内温度变化归因于未建模的蒸发冷却。Q4 的 R(t) 是附件指定的外部输入，不是由计算含水率预测的 R(C)；四组对照量化的是该指定几何情景的影响。

| 待检验过程区间 | 物理判断/竞争机制 | 必须记录的证据 | 结论成立要求 |
|---|---|---|---|
| 烘房升温与药材预热 | 环境变化、表面对流和内部导热共同决定温度滞后；同时已有水分迁移 | T_air、中心/表面 T、内部温差、平均含水率损失、热/质通量、响应尺度 | 按实际温湿响应解释，不能将“预热”写成完全不失水 |
| 温场接近稳定后的继续干燥 | 热响应可能快于水分响应，但温度对 D 的反馈仍可能影响达标时间 | max_r\|T−T_air\|、D 的径向范围、含水率变化率；完整耦合与受控冻结/准等温对照 | 温度偏差与对应 D、t_* 偏差均量化后，才称近似等温 |
| 后期低含水率区 | 低 C 对 D 的抑制可能形成表层瓶颈，也可能仍有显著边界交换阻力 | D(r,t)、C 梯度、内部/边界含水率降差、排湿通量、h_m/D 扰动响应 | 多项指标一致才认定某一机制主导；单个局部 Bi 不够 |
| 收缩显著及趋缓的区间 | R 减小改变扩散尺度、表面积/体积比；物性更换可抵消或加强几何效应 | R、Ṙ、2/R、α/D、收缩尺度、A/B/C/D 的场值和终点 | 分开报告几何、物性和交互项；阶段由 R 变化及状态诊断识别 |

以上为待检验假说，不预先宣布全部成立。题定的 30 min、3 h、4 h 数据终点和每 6 h 制表时刻均不是自动的物理阶段分界；不额外假定题目未支持的“恒速干燥段”。

补充低成本机制对照：在完整 Q23 基线通过验证后，分别冻结 D 的温度依赖、冻结热学物性对 C 的依赖，检查场值和 t_* 变化。其用途为判断耦合项重要性；冻结值、时刻和适用范围必须记录，人工冻结不是新的正式模型。均温判据诊断为必做，完整降阶加速实验仍可选。

### 2.2 无量纲与时间尺度分析必须影响模型决策

以初始半径 R0 作为内部扩散长度，定义 α=k/(ρcp)、τ_T=R0²/α、τ_C=R0²/D、Fo_T=αt/R0²、Fo_C=Dt/R0²、Λ=τ_C/τ_T=α/D，并记录 Bi_T=hR0/k、Bi_C=h_mR0/D。α/D 直接称热/质扩散率之比，不混用空气 Lewis 数的物性定义。

表面对流交换的数量级尺度单列为 τ_T,ext=ρcpV/(hA)、τ_C,ext=V/(h_mA)。忽略端面时 V/A=R/2。用于集总近似判别的长度 V/A 与此处定义径向 Bi 的 R 不相同，比较时必须换算。τ_T,ext 的集总响应解释有适用条件；在内部梯度显著时，它只提供外部交换尺度，不能直接替代全场加热时间。[MIT 瞬态传热讲义](https://web.mit.edu/course/16/16.unified/www/FALL/thermodynamics/notes/node129.html)

已由题给初态参数做出以下数量级核算，供制定仿真问题使用：

| 参数体系与状态 | α / (m²/s) | D / (m²/s) | τ_T | τ_C | α/D |
|---|---:|---:|---:|---:|---:|
| Q1，C=2.55 | 1.6886×10⁻⁷ | 4.9377×10⁻⁹ | 39.48 min | 22.50 h | 34.20 |
| Q23，C=2.55、T=28℃ | 1.4483×10⁻⁷ | 5.6417×10⁻⁹ | 46.03 min | 19.69 h | 25.67 |

Q1 的外部换热尺度约14.21 min，等效外部传质尺度约3.47 h；按初始扩散率计算，t=1800 s 的 Fo_T≈0.760、Fo_C≈0.022。这些量支持“内部热扩散比水分扩散快”的初步判断，但不代表整个药材已达到烘房温度，也不是烘干时长预测；边界变化、圆柱特征模态、阈值及变物性均会改变实际响应。

正式执行还需完成：

1. 推导非维化方程，明确选择热时间还是水分时间后另一方程前的小/大参数；由此解释刚性来源、为何保留空间场以及何时值得检查准等温近似。
2. 随计算状态记录 α(r,t)、D(r,t)、Λ 的范围及中心/表面代表值；用按实际状态累计的扩散尺度诊断代替“一次初值估算覆盖全程”。空间异质性不能被一个均值隐藏；全域累计 Fo 仅作诊断，不能套用常系数解析结论。
3. 对 Q23/Q4 的 D=D0 exp(−a/C)exp(−3850/T_K)，分析 ∂lnD/∂T_K=3850/T_K² 与 ∂lnD/∂C=a/C²。结合局部 T、C 变化率比较两项对 lnD 变化的贡献；Q4 在材料点上计算，避免把网格移动误认为状态变化。
4. 将内部/外部热响应尺度与附件1升温和平台变化的尺度比较；平台斜率接近零时不强行除以斜率。阶段边界由实际诊断量和阈值敏感性确定。
5. Q4 增加 τ_sh=R/|Ṙ| 和 Π_sh=R|Ṙ|/D，解释几何改变与扩散快慢的竞争；Ṙ=0 时按无收缩处理。Π_sh 是几何变化率指标，不是要求在材料坐标方程里另加对流项的依据；真正的 ALE 对流取决于 v−w。
6. 分析表层瓶颈时记录各段含水率梯度与通量，并用 h_m、D 的独立扰动作对照。瞬态内部通量随半径变化，不能未经准稳态检验就把各层视作承载同一流量的串联电阻。

产出 `reports/physics_and_scales.md`：每个保留/忽略项的理由、预期现象、反证条件、待计算诊断量，以及求解后对初始假说的核验结果。没有通过对照的假说必须修改，不能作为既定结论写入摘要。

## 3. 正式计算前必须固定的口径

### 3.1 边界、单位和输出时域

1. 计算中的长度用 m、时间用 s；温度状态可用 ℃，扩散公式内部转换为 K；干基含水率单位为 kg/kg。
2. 附件1在 0—14400 s 内分段线性插值。主情景在 4 h 后采用 3—4 h 的观测均值：温度约 49.998934℃、环境水分约 0.049988 kg/kg。正式配置保留完整均值精度。
3. 边界延拓对照分别用末次观测值、2.5—4 h 均值，两个温湿边界作为成对情景处理。若延拓在 4 h 存在小跳变，应明确记录并分段积分。
4. 在变量表分别定义空气含湿量 Y（按通常的干空气质量基准解释，题面未明确之处列为输入解释假设）、药材干基含水率 C（以干药材质量为参照）和等效边界 C_eq。附件1环境量暂记 Y；即使两者同写 kg/kg，也不能推出可直接相减。一般应有 C_eq=G(Y,T_air,…)；缺少吸附/解吸等温线时，主情景显式取 G(Y,T)=Y，作为题设下的有效边界闭合。
5. 对流换热、传质系数暂沿用附录二；主模型不增加未给参数的潜热、辐射或多相力学模块。
6. Q2 的论文表只取前 3 h；题面 Excel “完整结果”的时域存在解释空间。默认 `q2_export_end=full`，即输出至 Q3 临界干燥时刻，同时提供 `3h` 导出选项和独立备选文件。最终提交只使用一个明确声明的版本。
7. 输出采样间隔和求解时间步分离。1 s/60 s 是导出间隔，不能据此决定积分精度；0.1 cm 是导出空间间隔，不能代替内部细网格。

表面公式统一写作 −D∂C/∂n=h_m(C_s−C_eq)。其两侧是单位 m/s 的归一化含水率通量；若报告 kg/(m²·s) 的真实水分通量，还需按守恒模型乘以相应干固体密度。不得将该归一化通量直接当作质量损失速率。

边界闭合敏感性另设 C_eq=βY 的诊断情景，例如 β=0.9、1、1.1，并与 h_m 扰动分别记录。该小范围测试只能说明对所选映射偏移的敏感度，不能校准真实等温线或证明边界物理正确；没有内部观测时不声称已辨识 G 或 h_m。若结论对映射敏感，明确给出条件性结果，并将独立含水率/吸附数据列为补足验证所需信息。

潜热项缺参数是选择简化的依据，不是其影响很小的证据。计划中保留这一结构性局限；将来若补充外部物性估计，必须单列并核验来源，不悄然加入题设主模型。

### 3.2 Q4 材料坐标与守恒闭合

报告的材料坐标路线可作为主线，但其简化方程必须先通过以下推导。网格运动与物质运动需要分别定义，材料坐标和空间坐标并非同一种物理描述。[COMSOL 官方坐标说明](https://doc.comsol.com/6.3/doc/com.comsol.help.comsol/comsol_ref_deformedmeshes.34.05.html)

1. 定义干固体密度 ρ_d、水分比 C、水分质量通量 J_w、材料速度 v；从干固体连续方程和水分平衡出发。
2. 一般形式应先写为 ρ_d D_t C = −∇·J_w。采用 J_w=−ρ_d D∇C 时，得到 D_t C=(1/ρ_d)∇·(ρ_d D∇C)。
3. 假设长度不变、材料点径向仿射收缩 v_r=(Ṙ/R)r；在初始干固体密度均匀且无干固体流失的闭合下，ρ_d 随 R 的变化与干质量守恒保持一致。
4. 只有相应的空间均匀性与通量定义成立，才可简化到报告中的 u_t=(1/R²ξ)∂ξ(ξD u_ξ)。若采用空间变化的干固体密度，必须保留对应权重。
5. 明确附录中的经验密度 ρ(C) 如何用于热学系数，不能未经推导就将它等同于 ρ_d。若强行同时采用 ρ_d=ρ(C)/(1+C)、给定 R(t) 与均匀仿射收缩，可能发生闭合不一致，需要记录并修正所采用的简化解释。
6. 温度方程同样从选定能量模型变换，复核 1/R² 和表面梯度的 1/R。不能把材料运动项与单纯换元产生的网格项重复计入。
7. 只有上述简化成立时，才采用报告的归一化平衡式：平均含水率变化 + ∫[2h_m/R(t)](表面含水率−C_eq)dt≈0。若更换闭合，验证公式同步更换。

为避免重复计入运动，推导中必须出现以下可逐行检查的链式关系。设 u(ξ,t)=C(R(t)ξ,t)，网格速度 w=Ṙξ，则

\[
u_t=C_t|_r+wC_r,\qquad D_tC=C_t|_r+vC_r=u_t+\frac{v-w}{R}u_\xi.
\]

仅在仿射材料收缩且网格随材料运动时 v=w，材料导数才等于 u_t。若选择固定物质、移动计算网格或其他内部速度场，保留相对速度项并解释对应物理含义。各类时间导数由链式法则转换，不能仅因网格变形就引入两份运动项。[COMSOL 导数与坐标说明](https://doc.comsol.com/6.4/doc/com.comsol.help.comsol/comsol_ref_deformedmeshes.34.07.html)

必做三类排错算例：① R 恒定时退化为同物性的固定域方程；② D=0、h_m=0 的纯材料收缩中，预设非均匀 C 随材料保持不变，u(ξ,t)=u(ξ,0)，干固体质量与总水分质量均守恒；③ 为光滑指定 R(t)、T、C 构造源项/边界的制造解，独立核对运动符号、1/R、1/R²和体积权重。第二类尤其用于发现错误添加的稀释/压缩项：C 是干基比值，不能按单位体积浓度直接套用 −C∇·v。

这一步产出的是清楚列明成立条件的模型推导。数值上满足选定方程的平衡，不等于已经证明真实药材的完整热质力学模型。

### 3.3 半径函数与阈值口径

- 采用报告的分段线性 R(t) 为主情景，PCHIP 为对照；相较前次审题建议，这一选择便于忠实复现采样数据和开展插值敏感性分析。
- R(t) 数据覆盖 0—72 h。若 72 h 尚未达标，先报告数据支持范围内的状态，再单列“72 h 后半径固定为末值”的条件情景；不将额外假设产生的终点写成数据直接支持的答案。
- 事件定义为 g(t)=max_r C(r,t)−0.15。定位 g=0 的向下穿越，并验证之后 g<0。
- 连续模型中严格不等式通常没有一个被取到的最小达标时刻，因此报告临界时刻 t_*；需要可执行的离散时刻时，另列向上取整并复核后得到的首次达标秒。避免将“零点后一微小步”伪装成独立数学定义。

### 3.4 时间线与参数体系契约

| 任务 | 物理时间起点与初态 | 全时域物性/几何 | 可以继承的内容 | 必须禁止 |
|---|---|---|---|---|
| Q1 | t=0，T=28℃、C=2.55、R0=2 cm | 附录二，固定R0，求至1800 s | 公共输入函数和数值组件 | 将Q1结束等同于所有模型的物理阶段切换 |
| Q2 | 独立从同一t=0初态重算 | 附录三从t=0使用，固定R0 | 已核验组件 | 读取Q1末态，或1800 s时由附录二切换到三 |
| Q3 | 以Q2原始t=0为计时原点 | 与Q2完全相同 | 同配置全精度轨迹/状态，或从t=0等价重算 | 在Q2结束时重置时钟、读取舍入Excel继续积分 |
| Q4 | 独立从同一t=0初态重算 | 附录四从t=0使用，R(t)按附件2时间 | 几何/输入模块及核验方法 | 继承Q3干燥末态，或把R(t)时间错移 |

参数体系反映不同题问的建模规定，不代表要按题目编号把一次物理过程拼接成四段。建立参数版本、初态、物理时钟和缓存来源断言；增加故意使用错误末态/参数版本的失败检查，确保程序拒绝这类复用。

## 4. 分阶段任务与验收门槛

### 阶段 0：物理解释、尺度分析与模型契约

任务：先完成第2节的过程解释、初态与代表状态时间尺度、非维化方程，提出各阶段待检验机制；再完成守恒推导、边界闭合和第3.4节时间线契约。复用已完成的数据审计，补上输入校验和文件校验值。建立独立 Python 环境并记录实际依赖版本。初步计时只用于调整后续排程，不照搬报告中的机器耗时估计。

拟产物：`config/model_config.json`、`reports/physics_and_scales.md`、`reports/model_contract.md`、`reports/data_audit.md`、`reports/input_manifest.json`。

验收 G0：能够用一页解释“物理过程—变量反馈—尺度差异—建模取舍—待验证预测”；所有经验公式逐项对应题面；初值、边界、物性版本、时域、单位均有唯一解释；Q4 方程与验证用的守恒公式一致。推导若未自洽，先修正模型再开展正式生产计算；算例验证和已有独立任务可继续。

### 阶段 1：实现公共求解器

执行状态：Q1共享标量组件与`validation/kernel_checks.py`已完成；`src/coupled/`与`validation/q2_kernel_checks.py`已补齐Q23耦合及Q1退化检查。`src/moving/`和`validation/q4_kernel_checks.py`已完成Q4移动域、纯收缩、制造解及物理/参考域缩放核验，未修改此前已冻结内核。

任务：建立环形控制体几何、内部共享通量、中心对称条件、表面 Robin 条件和中心/表面重建。Q1、Q23、Q4 分别封装物性函数，共用空间离散、积分和输出接口。

内部传导首先使用距离加权调和平均。表面由末单元中心到真实边界的阻力与对流阻力联立求通量；非线性水分通量必要时求解标量表面方程，不能直接把末单元中心值当作表面值。

Kirchhoff 通量作为对照实现。Q1 的 D 只依赖 C，可用相应积分；Q2/Q4 的 D 还依赖 T，必须说明面温度近似或采用一致局部积分，不能直接宣称存在全局单变量精确变换。

拟产物：`src/data_io.py`、`src/properties.py`、`src/boundary.py`、`src/fvm.py`、`src/solver.py`、`src/reconstruction.py`、`validation/model_contract_checks.py`。

验收：常场平衡、零通量、常系数基准、内部通量抵消、中心极限、Robin 通量及重建均通过检查。负含水率和非有限结果触发诊断；不得通过无记录裁剪掩盖失稳。积分器试探状态需要数值保护时，必须验证它不影响被接受的物理解。

同时通过初态/参数版本断言和纯收缩算例。内部通量代数抵消只验证离散结构，仍需独立积分边界流量检查时间离散的累计平衡。

### 阶段 2：完成 Q1 与独立校验

执行状态：M1本地验收完成。均匀640格仍未充分解析第1秒表面水分层，已改为表面加密并采用1280格基线、2560格复核。完整导出点的热场相对Bessel参考最大差异1.2853×10⁻⁶ ℃；M4已补齐原题D(C)的独立P1弱形式抽检，水分稠密剖面最大差约1.32×10⁻⁶。历史结果文件、误差与机制见`reports/q1_acceptance.md`，补项见`reports/q1_nonlinear_independent.json`。

任务：用附录二分别求解热场、水分场至 1800 s；实现 Fourier–Bessel 热场半解析程序，并以 Duhamel/模态强迫处理随时间变化的边界，不能仅用常温边界解析式比较原题全过程。

初始网格 N=80、160、320；初始主容差 rtol=1e−7，温度/水分分别配置 atol。先做空间收敛，再在足够细网格上做时间容差对照；无需机械完成所有网格与容差的笛卡尔积。

拟产物：`scripts/question1.py`、`validation/bessel_q1.py`、全精度 Q1 缓存、论文表1—2、`result1.xlsx`、Q1 校验记录。

验收：在指定时空点和代表性连续剖面比较半解析解；检查中心/表面和初始边界不相容导致的早期快速变化；网格及容差误差有数值记录。

机制验收：将观测到的热响应、径向温差和水分迁移与第2.2节时间尺度比较，解释外部升温为何影响实际温度滞后；用数值证据说明预热期间水分变化程度。不同物性体系的尺度不得混用。

### 阶段 3：完成 Q2 耦合场和 Q3 临界时刻

执行状态：Q23全过程完成。Q2从原始初态重算，Q3沿10800 s全精度检查点保持物理时钟继续，临界时刻206906.491182 s；首次核验的严格达标整数秒206907 s。完整result2/result3分别逐项回读8690094和72429个数值；前三小时备选保留未改。错误时钟/检查点拒绝、非中心峰值排错、初态直算对照及阈值前后核验均通过。

已完成阶段5中适用于Q23的独立弱形式检查。前三小时后向Euler序列显示一阶时间误差缩减；补充自编变步BDF2，并延长至Q3阈值、单独加密网格与时间步。独立2560单元/3.75 s的温度与含水率抽检差异分别3.03×10⁻⁵ ℃和3.31×10⁻⁷ kg/kg。物理模型、边界解释及独立空间装配要求未变；很小的终点互差不掩盖相近空间误差可能抵消的风险。

任务：Q2 从 t=0 使用附录三重算，每次残差计算更新全部物性。积分到阈值事件，并由同一全精度轨迹输出 Q2 和 Q3，避免重复计算或读取四舍五入的 Excel 继续积分。

Q3 最大值检查覆盖单元、中心轴、真实表面和空间重建可能的内部极值；只有核验径向单调后才能将中心最湿写成结论。用事件检测及阈值局部加密控制 t_*，计算末期斜率与场误差到终点误差的传播。

保存 argmax_r C 的时间轨迹，检查阈值前后的全域状态，并对空间重建/极值搜索加密；定位首次向下过阈，单列必要的持续达标检查，不能用低频输出点替代连续事件判断。

拟产物：`scripts/question2.py`、`scripts/question3.py`、共享 Q23 缓存、论文表3—5、`result2.xlsx`、`result3.xlsx`、`reports/q23_validation.md`。

验收：Q2 0.5—3 h 的题定结果完整；Q3 全域首次达标判据成立；分开报告求根精度、时间积分误差、空间离散误差。不能因求根容差很小就认定干燥时间准确。

机制验收：用温度和含水率对 D 变化的贡献、温场平台诊断、受控冻结对照、表层/边界梯度与通量解释前中后期变化。若不能证明某环节占主导，使用“共同限制”等与证据相符的结论。事件时间误差传播近似仅在最大位置稳定且穿越斜率非零时采用；平坦/切触情形报告时间包络。

### 阶段 4：完成 Q4 与四组因素分离

执行状态：M3完成，实际结果与验证见`reports/q4_acceptance.md`、`q4_convergence.json`、`q4_independent_verification.json`、`q4_factorial_verification.json`。主配置1280格、2560格复核，最大步长30 s；主网格终点渐近误差尺度约0.0773 s，不是严格上界。原始初态复算数组逐元素一致，原始8文件及此前38项产物哈希未变。

四组临界时间A/B/C/D分别57.4740/25.2440/129.8484/51.0920 h。C为全程固定2 cm的明确反事实，72 h时max C=0.2246856而尚未达标；长期计算采用共同的环境均值假设，不外推观测收缩半径。A/B/D均在半径观测支持时域内终止。相对A的几何/物性/交互项分别−32.2301/+72.3744/−46.5263 h，总差−6.3820 h；不把总差全部归为收缩。

任务：采用阶段0核验的材料坐标方程、附录四物性，从初态计算；输出物理半径、中心/表面含水率、平均含水率及表面通量。首先检查 R(t)=R0 时模型退化到同物性固定域模型。

在相同边界、网格策略、容差和事件口径下运行：

| 场景 | 几何 | 物性 | 作用 |
|---|---|---|---|
| A | 固定 R0 | 附录三 | Q3 基线，可复用合格缓存 |
| B | 收缩 R(t) | 附录三 | 几何变化对照 |
| C | 固定 R0 | 附录四 | 物性变化对照 |
| D | 收缩 R(t) | 附录四 | Q4 正式模型 |

计算 Δgeom=tB−tA，Δprop=tC−tA，Δint=tD−tB−tC+tA，并验证 tD−tA=Δgeom+Δprop+Δint。它们是模型内、相对 A 基线的因素对照，不据此声称完成真实实验因果识别。

若采用实测收缩曲线的场景终点超出半径数据支持区间，必须明确统一的半径延拓情景后再分解。固定R0的反事实几何本身全时域有定义，不需要外推实测收缩曲线；但长期环境假设仍须与其余场景统一并声明，不能把反事实终点称为观测支持的实验结论。

已产物：`scripts/question4.py`、`scripts/factorial_q4.py`、论文表6、`results/final/result4.xlsx`、四组对照表及`reports/q4_acceptance.md`。守恒推导与数值验收合并于该报告，逐项证据保存在`reports/q4_*.json`，未另建重复的`q4_conservation.md`。

验收：参考域/物理域转换一致，质量平衡与推导相符，固定半径退化检验通过，因素分解闭合。域外输出空白，不填0，不复制表面值。

机制验收：用 R²/D、2/R 与收缩速率说明几何影响路径；四组对照说明物性更换是否抵消几何效应，不能仅凭Q4比Q3快/慢就归因于收缩。通过纯收缩、制造解及阶段5的独立移动域实现复核后，才冻结Q4正式结果。

### 阶段 5：统一精度、守恒和敏感性

执行状态：Q1标量基准、Q23全过程、Q4及四组因素对照已分别完成本地数值验证；M4补齐Q1原题非线性水分独立弱形式、Q23保存剖面重建比较、二维端面条件审查及270行统一误差台账。所要求的本次审查完成，不等于端面真实条件或其他结构性假设已被证明。

任务：对最终主场景开展 N=80、160、320 和 rtol=1e−6、1e−7、1e−8 的递进检查。若表面低扩散率区或终点仍不收敛，增加 N=640 或表面加密网格，再复核；不预先认定 N=320 足够。

时间离散分两项检查：在足够细空间网格上收紧 rtol/atol；再选取会实际约束步长的 H、H/2、H/4 上限，对代表性全过程/阈值区间进行步长敏感性检查并记录实际接受步长。如果改变 max_step 并未改变实际步序，就不能将相同结果计为有效的步长收敛证据。环境与半径插值折点、4 h延拓连接和事件附近应明确限制步长或分段积分，分别检查物理快速变化与输出插值误差。

增加独立参考实现：采用一维径向弱形式 FEM、独立边界装配与后向Euler时间离散，通过自身网格/步长加密后，抽检Q1并对Q23、Q4主情景计算至阈值。可共享原始输入、题设常数及已核验物性定义；不得共享主求解器的RHS、内部面通量、表面重建或运动项实现。Q4在独立实现中从弱形式重做坐标与权重变换。这项验证列为P1必做；大规模二维FEM/ALE仍是条件扩展，不依赖商业软件许可。

FVM+BDF换成FVM+Radau属于时间积分交叉检查，不算独立空间离散验证；Q1热场半解析吻合也不能替代Q23非线性水分场或Q4移动域验证。不同方法的差值应随各自加密减小并与联合误差估计相容；未解释的差异必须定位后才能交付。

验收指标：

- 记录全导出网格上 T、C 的最大差异、中心/表面误差、终点差异及四位小数舍入差异数量。
- 输出四位小数是格式要求。若要声称小数点后四位数值稳定，应至少检查连续两次加密的变化是否小于 5×10⁻⁵ 个对应单位，并检查舍入边界；这仍是数值证据而非严格误差上界。
- t_* 以小时输出时，5×10⁻⁵ h 为 0.18 s。先量化真实终点误差，再决定可声称的精度，不能用“一秒一行”推断四位小数精度。
- 质量平衡采用已冻结模型对应的体积/干质量权重与边界通量独立累积。时间积分平衡相对残差初始目标为 1e−6；同时报告归一化尺度，并检查容差收紧后残差变化。
- 对变 ρ、cp 的热方程，验证与所选方程一致的加权热量收支；不能直接以 ∑ρcpTV 的差代替严格总能量变化而漏掉物性变化项。
- 建立误差预算表，区分空间离散、时间积分、边界/输出重建、事件定位四类数值误差，以及参数、边界闭合和几何假设带来的模型差异。未验证独立性时不将这些差异按平方和合成“置信区间”。

敏感性必做：h、h_m 与 D 倍率各 ±10%；三种环境延拓；等效边界映射β情景；R(t)线性/PCHIP；第2.1节的受控冻结对照。分别解释热响应、外部传质、内部扩散和闭合假设的影响。先做单因素对照，只有出现明显交互或结论反转时才扩展 Morris/Sobol。扰动幅度是设定情景，不具有给定概率分布，结果称情景范围或敏感性区间。

拟产物：`validation/`下的半解析、制造解、独立弱形式及收敛脚本、`reports/convergence.csv`、`reports/independent_comparison.csv`、`reports/physics_diagnostics.csv`、`reports/sensitivity.csv`、`reports/error_budget.md`、`reports/verification.md`。

几何假设也需证据：比较径向R与轴向半长L/2的扩散尺度、端面传递范围及题定截面含义，给出忽略端面的可用范围。端面面积小不是充分证明；尺度判断不足以支持所需输出精度或全域判据时，升级二维轴对称验证。二维验证是检验一维几何假设，不是替代同一模型的独立程序验证。

本次执行说明：尺度不足以保证四位精度，已完成二维轴对称检查。采用成对封闭/暴露端面并分别加密径向、轴向和时间设置，避免直接用粗二维减细一维混入网格偏差；Q3这种错误比较甚至会反转效应方向。端面几何误差已由“完全未检查”转为“明确假设情景下定量比较”，但未知真实端面接触、各向异性等仍无普遍上界。

验收 G1：所有正式场景存在数值收敛、累计平衡、独立对照与误差预算记录。验收 G2：每条“主导机制/耦合影响/收缩贡献”论断都有诊断量或受控对照支持。任一未通过，则相关结果保持试算状态。

### 阶段 6：图表、论文与文献

模型推导随各问同步记录；最终数字、表格、图像在配置冻结后统一生成。

每问正文按“物理问题 → 假设与保留项 → 模型 → 数值求解 → 结果及机制解释 → 验证与局限”组织。FVM/BDF等实现细节主要进入求解小节或附录；摘要优先回答热/质响应为何分离、后期为何变慢、终点有多可信、收缩和物性各有何作用，且所有判断以实际结果为准。

优先制作：输入环境与半径概况、热/质响应和时间尺度对照、Q1 温湿径向剖面、Q2 变量反馈与时空场、Q3 最大含水率及阈值局部放大、经核验的低扩散率表层诊断、独立方法/网格/终点误差、敏感性、Q4 收缩场与四组因素分解。按支撑的结论组合多面板，不规定无依据的图像数量。

技术路线图按 `paper-diagram` 工作流产出可编辑 draw.io 与 PNG；数据图按 Python 科研绘图规范保存至 `figures/`。国赛图题简短，分析写正文。真实文献通过 `paper-search` 检索、核验 DOI 并生成 BibTeX；报告中的不可解析引用全部重新核对。

论文采用 `mma-paper` 的国赛模板与逐问组织方式。写作顺序为模型与结果、验证和评价、问题分析与假设、摘要。保留 AI 辅助使用和人工核验记录，提交格式按实际适用的官方文件核对，不能依据调研报告的占位引文认定规则已核实。

拟产物：`document.tex`、`book.bib`、`document.pdf`、`figures/`、`tables/`、支撑材料说明。

验收：正文每个结论对应计算或可核验来源；六张题定表齐全；没有把数值交叉验证写成实验验证；编译成功且无缺字、交叉引用缺失、明显溢出。

维护 `reports/claims_and_evidence.md`：逐条列出物理论断、适用题问/时段、对应诊断/图表、独立验证及局限。没有药材内部实测温湿数据时，只能声称对所选模型进行了程序验证、解的数值验证和情景分析；真实物理准确性仍受闭合假设约束。

### 阶段 7：导出和交付审计

由同一配置版本、同一批全精度结果生成最终文件。单独的审计程序重新打开 Excel，检查行列、单位、采样间隔、末行、四位小数格式、物理范围和与论文表的逐项一致性。

拟产物：`results/final/result1.xlsx`—`result4.xlsx`、可复现源码、依赖版本、`README.md`、`reports/final_audit.md`、论文源文件与 PDF。

验收：从原始附件可以重新生成全部交付；无手填替代计算值；输出可追溯到配置、缓存和运行日志。所有阶段完成状态与遗留局限明确记录。

最终审计逐项回查本文件开头的八项风险。出现时间线不符、全域事件漏检、未解释的坐标项、独立实现分歧、未收敛却声称四位精度，或缺少证据的机制结论，均须先修正。交付审计不得用“求解器成功退出”替代这些验收。

## 5. Excel 输出契约

| 文件 | 工作表 | 时间 | 距离 | 末行与特殊处理 |
|---|---|---|---|---|
| result1.xlsx | 温度、水分浓度 | 1—1800 s，每1 s | 0—2 cm，每0.1 cm，共21点 | 与题定表1—2一致 |
| result2.xlsx | 温度、水分浓度 | 默认全过程每1 s；可导出前三小时备选 | 同上 | 全过程定义为至 Q3 临界时刻；非整数秒终点另补末行并声明 |
| result3.xlsx | 保留模板原表名 | 从60 s起，每60 s | 同上 | 非60 s整数倍的临界时刻另补末行 |
| result4.xlsx | 保留模板原表名 | 从60 s起，每60 s | 固定物理位置0—2 cm，每0.1 cm；另列药材表面 | r>R(t)留空；表面列始终对应当前R(t)；补临界末行 |

论文表6同样使用固定物理距离和动态表面；必要时附该行 R(t) 说明。求解与缓存保留 t=0；提交模板按示例从第一个正采样时刻起。补充半径信息可放独立文件，不随意修改模板数据区结构。

Q2 全过程双工作表可能包含数百万个结果值。采用分块重建、顺序写入与写后读回审计，避免一次在内存中生成“全部内部网格×全部秒”的大表。二进制全精度缓存与 Excel 展示格式分别管理。

## 6. 配置、目录与复用

统一配置至少包含：输入路径/校验值、单位/参照质量基准、物理计时原点、初态与允许的缓存来源、物性版本、边界映射版本、网格与加密方式、积分器、rtol/atol、最大步长/折点处理、通量方案、环境插值/延拓窗口、半径插值/延拓、材料速度与网格速度约定、干质量权重、阈值、Q2导出时域、输出舍入规则、情景编号和随机种子（若使用随机采样）。阶段判别阈值和误差验收尺度同样记录，不在看过结果后无记录调整。

建议目录：

```text
Workspace/
├── 执行计划.md
├── config/                  # 模型与场景配置
├── data/raw/                # 输入副本及校验记录
├── src/                     # 可复用几何、物性、求解、重建、导出组件
├── scripts/question1.py
├── scripts/question2.py
├── scripts/question3.py
├── scripts/question4.py
├── scripts/factorial_q4.py
├── validation/              # 半解析、制造解、独立FEM、收敛、守恒、敏感性
├── results/cache/           # 全精度状态与元信息
├── results/final/           # 四个正式 Excel
├── results/alternatives/    # 格式或边界情景备选，分开标记
├── reports/                 # 审计、误差和执行记录
├── figures/
├── tables/
├── document.tex
├── book.bib
└── README.md
```

缓存键绑定完整配置与输入校验值。A场景复用Q23已验证缓存，Q2/Q3共享轨迹；配置或离散方案改变后不能沿用失配缓存。

## 7. 优先级与进度控制

| 优先级 | 内容 | 完成原则 |
|---|---|---|
| P0 必做 | 物理解释与无量纲尺度、边界/时间线契约、守恒推导、公共求解器、四问结果、六张题定表、四个Excel | 先形成自洽模型与试算，再用P1验收正式结果 |
| P1 必做 | 机制诊断与冻结对照、Q1半解析、Q4制造解/纯收缩、独立弱形式Q23/Q4验证、网格/容差/有效步长收敛、平衡、终点误差与敏感性、因素分离、论文与审计 | 与逐问计算交错执行；相关验证通过后才冻结结果 |
| P2 条件扩展 | 大规模二维轴对称/FEM/ALE、完整准等温降阶加速、全局敏感性 | 根据实际风险与收益选择；一维几何依据不足时二维验证提前升级 |
| 本轮不纳入 | PINN、神经算子代理、完整多相热湿力耦合 | 当前材料不支持相应投入的必要性；用户另行提出再规划 |

二维验证若发现端部影响足以改变所需结果或结论，就升级为必须解决的问题，不能因原先标记“选做”而忽略。端面面积占比只提供几何直觉，不是忽略端面误差的定量证明。

进度按五个可审阅里程碑汇报：M0 物理解释、尺度与模型契约；M1 Q1机制解释和半解析；M2 Q23机制诊断、全域事件和独立对照；M3 Q4运动/守恒验证和因素分离；M4完整误差预算、证据对应的论文、结果文件及八项风险终审。每次给出已完成事项、验证证据和尚存问题。各问本地验收与阶段5综合复核交错开展，不能先写完结论再补验证。

未提供截止时间，因此采用依赖顺序和验收门槛排程。首轮代表性求解及导出计时后，再给出可信的计算工期；报告中的分钟级耗时仅为参考。
